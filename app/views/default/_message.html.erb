<% flash.each do |key, value| %>
      <%= puts key, value %>
      <div class=" <%= flash_class(key) %>">
      <a href="#" data-dismiss="alert" class="close">×</a>  
        <%= value %>
      </div>
<% end %>